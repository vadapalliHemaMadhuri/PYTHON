Created a simple calucualtor by using dictionary, whileloop and functions.


def add(n1,n2):
    return n1+n2
def subtract(n1,n2):
    return n1-n2
def divide(n1,n2):
    return n1/n2
def multiply(n1,n2):
    return n1*n2
operations={"+":add,"-":subtract,"/":divide,"*":multiply}

def caluculator():
    end=True
    num1=int(input("what is your first number?"))

    for symbols in operations:
        print(symbols)
    while end:    
        operand=input("Enter the operand that do required operation? ")
        num2=int(input("what is your second number?"))
        function=operations[operand]
        answer1=function(num1,num2)
        print(f"{num1} {operand} {num2} = {answer1}")   
        n=input(f"Type yes to caluculate with{answer1} or type new if you want new caluculation: ")
        if n=="yes":
            num1=answer1
        else:
            end=False
            caluculator()
caluculator() 
